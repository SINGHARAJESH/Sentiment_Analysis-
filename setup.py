from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project where to find emotion based on given text',
    author='Rajesh Singha',
    license='',
)
